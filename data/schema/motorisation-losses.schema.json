{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "motorisation-losses.schema.json",
  "title": "Abstract Truck / Motorisation Point Loss Chart (SPI 32.59; headed 58.5 on the chart sheet)",
  "type": "object", "required": ["table", "sources", "rows"], "additionalProperties": false,
  "properties": {
    "table": {"const": "motorisation-losses"},
    "sources": {"$ref": "common.schema.json#/$defs/sources"},
    "notes": {"type": "array", "items": {"type": "string"}},
    "rows": {"type": "array", "minItems": 1, "items": {
      "type": "object", "required": ["month", "cw_percent", "axis_percent", "sources"], "additionalProperties": false,
      "properties": {
        "month": {"type": "string", "pattern": "^194[0-3]-(0[1-9]|1[0-2])$", "description": "Month the losses are for (YYYY-MM); months the chart prints as a dash are omitted"},
        "cw_percent": {"type": "integer", "minimum": 0, "maximum": 100, "description": "Share of Commonwealth motorisation points in North Africa lost"},
        "axis_percent": {"type": "integer", "minimum": 0, "maximum": 100, "description": "Share of Axis motorisation points in North Africa (the Tripoli/Tunis boxes included) lost"},
        "sources": {"$ref": "common.schema.json#/$defs/sources"}}}}
  }
}
