{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "prisoners-captured.schema.json",
  "title": "Prisoners Captured Results Table (SPI 15.89)",
  "type": "object", "required": ["table", "sources", "rows"], "additionalProperties": false,
  "properties": {
    "table": {"const": "prisoners-captured"},
    "sources": {"$ref": "common.schema.json#/$defs/sources"},
    "notes": {"type": "array", "items": {"type": "string"}},
    "rows": {"type": "array", "minItems": 1, "items": {
      "type": "object", "required": ["die", "percent", "sources"], "additionalProperties": false,
      "properties": {"die": {"$ref": "die-lookup.schema.json#/$defs/dieRange"},
                     "percent": {"type": "integer", "minimum": 0, "maximum": 100,
                                 "description": "Share of the infantry, armour and gun losses just taken that are prisoners"},
                     "sources": {"$ref": "common.schema.json#/$defs/sources"}}}}
  }
}
